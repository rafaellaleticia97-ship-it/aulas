
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Equações do 2º Grau — Fatoração | 9º Ano</title>
<link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&family=Baloo+2:wght@700;800&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #faf8f4;
    --card: #ffffff;
    --primary: #1a5276;
    --accent: #e67e22;
    --green: #1e8449;
    --red: #c0392b;
    --purple: #7d3c98;
    --yellow: #f4d03f;
    --soft: #eaf0fb;
    --border: #d5e8f8;
    --text: #1a1a2e;
    --muted: #5d6d7e;
    --shadow: 0 4px 24px rgba(26,82,118,0.10);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: 'Nunito', sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* FLOATING SYMBOLS */
  .bg-symbols {
    position: fixed;
    inset: 0;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }
  .sym {
    position: absolute;
    font-family: 'Baloo 2', sans-serif;
    font-weight: 700;
    opacity: 0.06;
    color: var(--primary);
    animation: floatSym linear infinite;
    user-select: none;
  }
  @keyframes floatSym {
    0% { transform: translateY(110vh) rotate(0deg); }
    100% { transform: translateY(-20vh) rotate(360deg); }
  }

  /* LAYOUT */
  .wrapper {
    position: relative;
    z-index: 1;
    max-width: 1100px;
    margin: 0 auto;
    padding: 32px 24px 60px;
  }

  /* HEADER */
  .header {
    text-align: center;
    margin-bottom: 40px;
  }
  .header .tag {
    display: inline-block;
    background: var(--primary);
    color: #fff;
    font-size: 1rem;
    font-weight: 800;
    letter-spacing: 2px;
    padding: 6px 22px;
    border-radius: 30px;
    margin-bottom: 14px;
    text-transform: uppercase;
  }
  .header h1 {
    font-family: 'Baloo 2', sans-serif;
    font-size: clamp(2rem, 5vw, 3.4rem);
    color: var(--primary);
    line-height: 1.15;
    margin-bottom: 10px;
  }
  .header p {
    font-size: clamp(1rem, 2.5vw, 1.25rem);
    color: var(--muted);
    font-weight: 600;
  }

  /* PROGRESS NAV */
  .progress-nav {
    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
    margin-bottom: 36px;
  }
  .step-btn {
    background: var(--card);
    border: 2.5px solid var(--border);
    color: var(--muted);
    font-family: 'Nunito', sans-serif;
    font-size: clamp(0.85rem, 1.8vw, 1rem);
    font-weight: 700;
    padding: 10px 20px;
    border-radius: 30px;
    cursor: pointer;
    transition: all 0.25s;
    white-space: nowrap;
  }
  .step-btn:hover { border-color: var(--primary); color: var(--primary); }
  .step-btn.active {
    background: var(--primary);
    border-color: var(--primary);
    color: #fff;
    box-shadow: 0 4px 16px rgba(26,82,118,0.25);
  }
  .step-btn.done {
    background: var(--green);
    border-color: var(--green);
    color: #fff;
  }

  /* SLIDE */
  .slide { display: none; animation: fadeIn 0.4s ease; }
  .slide.active { display: block; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(16px); } to { opacity: 1; transform: translateY(0); } }

  /* CARD */
  .card {
    background: var(--card);
    border-radius: 20px;
    padding: clamp(24px, 4vw, 44px);
    box-shadow: var(--shadow);
    margin-bottom: 24px;
    border: 1.5px solid var(--border);
  }
  .card-title {
    font-family: 'Baloo 2', sans-serif;
    font-size: clamp(1.3rem, 3vw, 2rem);
    color: var(--primary);
    margin-bottom: 18px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .card-title .icon {
    background: var(--soft);
    width: 48px; height: 48px;
    border-radius: 14px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.5rem;
    flex-shrink: 0;
  }

  /* TEXT */
  p.txt {
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    line-height: 1.75;
    color: var(--text);
    margin-bottom: 14px;
  }
  p.txt:last-child { margin-bottom: 0; }

  /* HIGHLIGHT BOX */
  .box {
    border-radius: 14px;
    padding: 18px 22px;
    margin: 18px 0;
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    font-weight: 700;
    line-height: 1.7;
  }
  .box-blue { background: #dbeafe; border-left: 5px solid var(--primary); color: #1e3a5f; }
  .box-orange { background: #fdebd0; border-left: 5px solid var(--accent); color: #784212; }
  .box-green { background: #d5f5e3; border-left: 5px solid var(--green); color: #145a32; }
  .box-yellow { background: #fef9e7; border-left: 5px solid #d4ac0d; color: #7d6608; }
  .box-red { background: #fadbd8; border-left: 5px solid var(--red); color: #641e16; }
  .box-purple { background: #e8daef; border-left: 5px solid var(--purple); color: #4a235a; }

  /* MATH DISPLAY */
  .math-block {
    background: var(--soft);
    border: 2px solid var(--border);
    border-radius: 16px;
    padding: 20px 28px;
    text-align: center;
    font-size: clamp(1.3rem, 3.5vw, 2rem);
    font-weight: 800;
    color: var(--primary);
    margin: 20px 0;
    letter-spacing: 0.5px;
    font-family: 'Baloo 2', sans-serif;
  }
  .math-block .label {
    font-family: 'Nunito', sans-serif;
    font-size: clamp(0.75rem, 1.5vw, 0.9rem);
    font-weight: 700;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 1.5px;
    display: block;
    margin-bottom: 6px;
  }

  /* STEPS */
  .steps { display: flex; flex-direction: column; gap: 16px; margin: 20px 0; }
  .step-item {
    display: flex;
    gap: 18px;
    align-items: flex-start;
    background: var(--soft);
    border-radius: 14px;
    padding: 18px 20px;
    border: 1.5px solid var(--border);
  }
  .step-num {
    background: var(--primary);
    color: #fff;
    font-family: 'Baloo 2', sans-serif;
    font-size: 1.3rem;
    font-weight: 800;
    width: 42px; height: 42px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    flex-shrink: 0;
  }
  .step-content { flex: 1; }
  .step-content strong {
    display: block;
    font-size: clamp(1rem, 2.2vw, 1.15rem);
    color: var(--primary);
    margin-bottom: 6px;
  }
  .step-content span {
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    color: var(--text);
    line-height: 1.6;
  }

  /* EXAMPLE REVEAL */
  .reveal-area { margin: 20px 0; }
  .reveal-btn {
    background: var(--accent);
    color: #fff;
    font-family: 'Nunito', sans-serif;
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    font-weight: 800;
    border: none;
    border-radius: 12px;
    padding: 14px 28px;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 14px;
  }
  .reveal-btn:hover { background: #d35400; transform: translateY(-2px); }
  .reveal-btn:active { transform: translateY(0); }
  .reveal-content {
    display: none;
    animation: fadeIn 0.35s ease;
  }
  .reveal-content.open { display: block; }

  /* GRID 2 col */
  .grid2 { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin: 20px 0; }
  @media (max-width: 680px) { .grid2 { grid-template-columns: 1fr; } }

  /* ATTENTION BOX */
  .attention {
    background: #fff3cd;
    border: 2.5px solid #f0ad4e;
    border-radius: 14px;
    padding: 16px 20px;
    display: flex;
    gap: 14px;
    align-items: flex-start;
    margin: 18px 0;
    font-size: clamp(0.95rem, 2vw, 1.1rem);
    line-height: 1.65;
    font-weight: 600;
    color: #7d5100;
  }
  .attention .att-icon { font-size: 1.6rem; flex-shrink: 0; }

  /* TEA tip */
  .tea-tip {
    background: #e8f8f5;
    border: 2px solid #76d7c4;
    border-radius: 14px;
    padding: 14px 18px;
    display: flex;
    gap: 12px;
    align-items: flex-start;
    margin: 14px 0;
    font-size: clamp(0.9rem, 1.9vw, 1.05rem);
    color: #1a5244;
    font-weight: 600;
    line-height: 1.6;
  }
  .tea-tip .t-icon { font-size: 1.4rem; flex-shrink: 0; }

  /* NAV BUTTONS */
  .nav-btns {
    display: flex;
    justify-content: space-between;
    gap: 16px;
    margin-top: 30px;
    flex-wrap: wrap;
  }
  .nav-btn {
    font-family: 'Nunito', sans-serif;
    font-size: clamp(1rem, 2vw, 1.15rem);
    font-weight: 800;
    padding: 14px 32px;
    border-radius: 14px;
    border: none;
    cursor: pointer;
    transition: all 0.2s;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .nav-btn.prev { background: var(--soft); color: var(--primary); border: 2px solid var(--border); }
  .nav-btn.next { background: var(--primary); color: #fff; box-shadow: 0 4px 16px rgba(26,82,118,0.25); }
  .nav-btn:hover { transform: translateY(-2px); }
  .nav-btn:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }

  /* FATOR TABLE */
  .fator-table { width: 100%; border-collapse: collapse; margin: 16px 0; font-size: clamp(0.95rem, 2vw, 1.1rem); }
  .fator-table th { background: var(--primary); color: #fff; padding: 12px 16px; text-align: left; font-weight: 800; }
  .fator-table td { padding: 11px 16px; border-bottom: 1px solid var(--border); }
  .fator-table tr:nth-child(even) td { background: var(--soft); }
  .fator-table .highlight td { background: #d5f5e3 !important; font-weight: 700; color: var(--green); }

  /* CHIP */
  .chip {
    display: inline-block;
    background: var(--soft);
    border: 1.5px solid var(--border);
    border-radius: 8px;
    padding: 4px 14px;
    font-weight: 700;
    font-size: clamp(0.9rem, 1.9vw, 1.05rem);
    color: var(--primary);
    margin: 3px;
  }

  /* PRACTICE */
  .practice-card {
    background: #f0f8ff;
    border: 2px solid #b0d4f1;
    border-radius: 16px;
    padding: 22px 26px;
    margin: 16px 0;
  }
  .practice-card h3 {
    font-family: 'Baloo 2', sans-serif;
    font-size: clamp(1.1rem, 2.5vw, 1.4rem);
    color: var(--primary);
    margin-bottom: 12px;
  }

  .divider {
    border: none;
    border-top: 2px dashed var(--border);
    margin: 28px 0;
  }

  /* FINAL */
  .final-card {
    text-align: center;
    padding: 50px 30px;
  }
  .final-card .big-emoji { font-size: 5rem; margin-bottom: 16px; }
  .final-card h2 {
    font-family: 'Baloo 2', sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.8rem);
    color: var(--primary);
    margin-bottom: 12px;
  }
  .final-card p {
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    color: var(--muted);
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.7;
  }

  /* ARROW */
  .arrow-flow {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    flex-wrap: wrap;
    margin: 18px 0;
    font-size: clamp(1rem, 2.2vw, 1.2rem);
    font-weight: 700;
  }
  .arrow-box {
    background: var(--soft);
    border: 2px solid var(--border);
    border-radius: 12px;
    padding: 10px 18px;
    color: var(--primary);
    font-family: 'Baloo 2', sans-serif;
    font-size: clamp(1rem, 2.2vw, 1.3rem);
    text-align: center;
  }
  .arrow-sym { font-size: 1.8rem; color: var(--accent); }

</style>
</head>
<body>

<!-- FLOATING SYMBOLS -->
<div class="bg-symbols" id="bgSyms"></div>

<div class="wrapper">

  <!-- HEADER -->
  <div class="header">
    <div class="tag">9º Ano · Matemática</div>
    <h1>Equações do 2º Grau<br>e Fatoração</h1>
    <p>Aula passo a passo · Capítulo 5.2</p>
  </div>

  <!-- PROGRESS NAV -->
  <div class="progress-nav" id="progressNav">
    <button class="step-btn active" onclick="goTo(0)">1 · O que é fatorar?</button>
    <button class="step-btn" onclick="goTo(1)">2 · Fator comum</button>
    <button class="step-btn" onclick="goTo(2)">3 · Diferença de quadrados</button>
    <button class="step-btn" onclick="goTo(3)">4 · Trinômio → fatoração</button>
    <button class="step-btn" onclick="goTo(4)">5 · Produto zero</button>
    <button class="step-btn" onclick="goTo(5)">6 · Praticando</button>
  </div>

  <!-- ===== SLIDE 0: O que é fatorar ===== -->
  <div class="slide active" id="slide-0">
    <div class="card">
      <div class="card-title"><div class="icon">🔍</div> O que significa fatorar?</div>

      <p class="txt">Você já aprendeu que um número pode ser escrito como <strong>produto de fatores</strong>.</p>

      <div class="math-block">
        <span class="label">Exemplo com números</span>
        72 = 2 × 2 × 2 × 3 × 3 = 2³ · 3²
      </div>

      <p class="txt">Na álgebra, fazemos a mesma coisa com <strong>expressões algébricas</strong>: reescrevemos como um produto de partes menores.</p>

      <div class="box box-blue">
        📌 <strong>Fatorar</strong> = reescrever uma expressão como um <strong>produto de fatores</strong>
      </div>

      <div class="tea-tip">
        <span class="t-icon">🧩</span>
        <span>Pense como um <strong>quebra-cabeça</strong>: você tem a peça montada (a expressão completa) e quer descobrir as <strong>peças separadas</strong> que a formam.</span>
      </div>

      <p class="txt">Hoje vamos ver <strong>3 formas de fatorar</strong> e entender como isso ajuda a resolver equações do 2º grau.</p>

      <div class="arrow-flow">
        <div class="arrow-box">Expressão<br>algébrica</div>
        <span class="arrow-sym">→</span>
        <div class="arrow-box">Fatoração</div>
        <span class="arrow-sym">→</span>
        <div class="arrow-box">Produto<br>de fatores</div>
        <span class="arrow-sym">→</span>
        <div class="arrow-box">Raízes da<br>equação ✓</div>
      </div>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" disabled>← Anterior</button>
      <button class="nav-btn next" onclick="goTo(1)">Próximo →</button>
    </div>
  </div>

  <!-- ===== SLIDE 1: Fator comum ===== -->
  <div class="slide" id="slide-1">
    <div class="card">
      <div class="card-title"><div class="icon">1️⃣</div> Fator comum em evidência</div>

      <p class="txt">Quando todos os termos de uma expressão têm um <strong>fator em comum</strong>, podemos colocá-lo "para fora".</p>

      <div class="box box-orange">
        🔑 <strong>Regra:</strong> Se todos os termos têm x, coloque o x em evidência!
      </div>

      <div class="steps">
        <div class="step-item">
          <div class="step-num">1</div>
          <div class="step-content">
            <strong>Observe a expressão</strong>
            <span>x² + 2x<br>Quais fatores aparecem nos <em>dois</em> termos?</span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">2</div>
          <div class="step-content">
            <strong>Identifique o fator comum</strong>
            <span>x² = <strong>x</strong> · x &nbsp;&nbsp;&nbsp; 2x = <strong>x</strong> · 2<br>O fator comum é: <strong>x</strong></span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">3</div>
          <div class="step-content">
            <strong>Coloque em evidência</strong>
            <span>x² + 2x = <strong>x · (x + 2)</strong></span>
          </div>
        </div>
      </div>

      <div class="math-block">
        <span class="label">Forma fatorada</span>
        x² + 2x = x · (x + 2)
      </div>

      <div class="tea-tip">
        <span class="t-icon">✅</span>
        <span>Para <strong>conferir</strong>: distribua o x de volta: x · (x + 2) = x² + 2x. Se der a expressão original, está certo!</span>
      </div>

      <hr class="divider">

      <div class="reveal-area">
        <button class="reveal-btn" onclick="toggleReveal('r1')">▶ Ver mais um exemplo</button>
        <div class="reveal-content" id="r1">
          <div class="math-block">
            <span class="label">Fatorar x² + 8x</span>
            x² + 8x = x · (x + 8)
          </div>
          <div class="box box-green">
            ✔ Conferindo: x · (x + 8) = x² + 8x ✓
          </div>
        </div>
      </div>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" onclick="goTo(0)">← Anterior</button>
      <button class="nav-btn next" onclick="goTo(2)">Próximo →</button>
    </div>
  </div>

  <!-- ===== SLIDE 2: Diferença de quadrados ===== -->
  <div class="slide" id="slide-2">
    <div class="card">
      <div class="card-title"><div class="icon">2️⃣</div> Diferença de quadrados</div>

      <p class="txt">Você já conhece esse produto notável:</p>

      <div class="math-block">
        (a + b) · (a − b) = a² − b²
      </div>

      <p class="txt">Então podemos fazer o <strong>caminho inverso</strong>: se vemos a² − b², já sabemos os fatores!</p>

      <div class="box box-blue">
        📌 <strong>Fórmula:</strong> &nbsp; a² − b² = (a + b) · (a − b)
      </div>

      <div class="steps">
        <div class="step-item">
          <div class="step-num">1</div>
          <div class="step-content">
            <strong>Identifique os dois quadrados</strong>
            <span>x² − 25 &nbsp;→&nbsp; x² é x², e 25 é 5²<br>Logo: a = x e b = 5</span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">2</div>
          <div class="step-content">
            <strong>Aplique a fórmula</strong>
            <span>x² − 25 = x² − 5² = (x + 5) · (x − 5)</span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">3</div>
          <div class="step-content">
            <strong>Confira</strong>
            <span>(x + 5) · (x − 5) = x² − 5x + 5x − 25 = x² − 25 ✓</span>
          </div>
        </div>
      </div>

      <hr class="divider">

      <div class="grid2">
        <div>
          <p class="txt" style="font-weight:700; color:var(--primary);">Mais exemplos:</p>
          <div class="reveal-area">
            <button class="reveal-btn" onclick="toggleReveal('r2a')" style="background:var(--purple);">▶ 49 − a²</button>
            <div class="reveal-content" id="r2a">
              <div class="math-block" style="font-size:1.3rem;">
                49 − a² = 7² − a²<br>= (7 + a) · (7 − a)
              </div>
            </div>
          </div>
        </div>
        <div>
          <p class="txt" style="font-weight:700; color:var(--primary);">&nbsp;</p>
          <div class="reveal-area">
            <button class="reveal-btn" onclick="toggleReveal('r2b')" style="background:var(--green);">▶ x² − 1/9</button>
            <div class="reveal-content" id="r2b">
              <div class="math-block" style="font-size:1.3rem;">
                x² − 1/9 = x² − (1/3)²<br>= (x + 1/3) · (x − 1/3)
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="attention">
        <span class="att-icon">⚠️</span>
        <span>Só funciona com <strong>subtração</strong>! Se for a² + b², <strong>não</strong> dá para fatorar dessa forma.</span>
      </div>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" onclick="goTo(1)">← Anterior</button>
      <button class="nav-btn next" onclick="goTo(3)">Próximo →</button>
    </div>
  </div>

  <!-- ===== SLIDE 3: Trinômio ===== -->
  <div class="slide" id="slide-3">
    <div class="card">
      <div class="card-title"><div class="icon">3️⃣</div> Fatorando o trinômio</div>

      <p class="txt">Agora vamos fatorar expressões do tipo <strong>x² + bx + c</strong>, que têm <em>três</em> termos.</p>

      <div class="box box-blue">
        💡 <strong>Ideia:</strong> procuramos dois números que, ao mesmo tempo:<br>
        &nbsp;&nbsp;&nbsp;✔ <strong>multiplicados</strong>, dão o termo independente (c)<br>
        &nbsp;&nbsp;&nbsp;✔ <strong>somados</strong>, dão o coeficiente de x (b)
      </div>

      <div class="tea-tip">
        <span class="t-icon">🧩</span>
        <span>É como um <strong>jogo de adivinhação</strong> com dois números. Siga sempre os 3 passos abaixo!</span>
      </div>

      <hr class="divider">

      <p class="txt" style="font-weight:800; color:var(--primary); font-size:1.2rem;">📌 Exemplo: x² + 7x + 12</p>

      <div class="steps">
        <div class="step-item">
          <div class="step-num">1</div>
          <div class="step-content">
            <strong>Liste pares que multiplicam para dar 12</strong>
            <span>
              1 × 12 = 12 &nbsp;&nbsp; | &nbsp;&nbsp; 2 × 6 = 12 &nbsp;&nbsp; | &nbsp;&nbsp; <strong style="color:var(--green)">3 × 4 = 12</strong>
            </span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">2</div>
          <div class="step-content">
            <strong>Qual par soma 7?</strong>
            <span>
              1 + 12 = 13 ✗ &nbsp;&nbsp; | &nbsp;&nbsp; 2 + 6 = 8 ✗ &nbsp;&nbsp; | &nbsp;&nbsp; <strong style="color:var(--green)">3 + 4 = 7 ✓</strong>
            </span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">3</div>
          <div class="step-content">
            <strong>Monte a forma fatorada</strong>
            <span>x² + 7x + 12 = <strong>(x + 3) · (x + 4)</strong></span>
          </div>
        </div>
      </div>

      <div class="math-block">
        <span class="label">Forma fatorada</span>
        x² + 7x + 12 = (x + 3) · (x + 4)
      </div>

      <hr class="divider">

      <p class="txt" style="font-weight:800; color:var(--primary); font-size:1.2rem;">📌 Exemplo com números negativos: x² − 8x + 15</p>

      <div class="reveal-area">
        <button class="reveal-btn" onclick="toggleReveal('r3')">▶ Ver a resolução</button>
        <div class="reveal-content" id="r3">
          <table class="fator-table">
            <tr><th>Pares com produto 15</th><th>Soma</th><th>É −8?</th></tr>
            <tr><td>1 e 15</td><td>16</td><td>✗</td></tr>
            <tr><td>3 e 5</td><td>8</td><td>✗</td></tr>
            <tr class="highlight"><td>−3 e −5</td><td>−8</td><td>✓</td></tr>
          </table>
          <div class="math-block">
            x² − 8x + 15 = (x − 3) · (x − 5)
          </div>
          <div class="box box-green">✔ Conferindo: (x−3)(x−5) = x² − 5x − 3x + 15 = x² − 8x + 15 ✓</div>
        </div>
      </div>

      <div class="attention">
        <span class="att-icon">⚠️</span>
        <span>Quando o termo independente é <strong>positivo</strong> e o do meio é <strong>negativo</strong>, os dois números do par devem ser <strong>negativos</strong>.</span>
      </div>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" onclick="goTo(2)">← Anterior</button>
      <button class="nav-btn next" onclick="goTo(4)">Próximo →</button>
    </div>
  </div>

  <!-- ===== SLIDE 4: Produto zero ===== -->
  <div class="slide" id="slide-4">
    <div class="card">
      <div class="card-title"><div class="icon">⚡</div> Produto zero = encontrar as raízes!</div>

      <p class="txt">Agora vem a parte mais poderosa. Uma vez que fatoramos, podemos <strong>resolver a equação</strong>!</p>

      <div class="box box-purple">
        🔑 <strong>Lei do produto zero:</strong> Se A · B = 0, então A = 0 <em>ou</em> B = 0.<br>
        Um produto só é zero quando <em>pelo menos um</em> dos fatores é zero.
      </div>

      <div class="tea-tip">
        <span class="t-icon">💡</span>
        <span>Pense assim: se você multiplicou dois números e o resultado foi <strong>zero</strong>, pelo menos um deles <em>tem</em> que ser zero. Não tem como dois números diferentes de zero se multiplicarem e dar zero!</span>
      </div>

      <hr class="divider">

      <p class="txt" style="font-weight:800; color:var(--primary); font-size:1.2rem;">📌 Resolvendo x² + 7x + 12 = 0</p>

      <div class="steps">
        <div class="step-item">
          <div class="step-num">1</div>
          <div class="step-content">
            <strong>Fatorar</strong>
            <span>x² + 7x + 12 = <strong>(x + 3) · (x + 4)</strong></span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">2</div>
          <div class="step-content">
            <strong>Aplicar produto zero</strong>
            <span>(x + 3) · (x + 4) = 0<br>→ x + 3 = 0 &nbsp;<strong>ou</strong>&nbsp; x + 4 = 0</span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">3</div>
          <div class="step-content">
            <strong>Resolver cada equação simples</strong>
            <span>x + 3 = 0 → <strong>x = −3</strong><br>x + 4 = 0 → <strong>x = −4</strong></span>
          </div>
        </div>
        <div class="step-item">
          <div class="step-num">4</div>
          <div class="step-content">
            <strong>Conferir — substituindo x = −3</strong>
            <span>(−3)² + 7·(−3) + 12 = 9 − 21 + 12 = <strong>0 ✓</strong></span>
          </div>
        </div>
      </div>

      <div class="math-block">
        <span class="label">Solução</span>
        S = {−3 ; −4}
      </div>

      <hr class="divider">

      <p class="txt" style="font-weight:800; color:var(--primary); font-size:1.2rem;">📌 Fator em evidência: x² + 8x = 0</p>

      <div class="reveal-area">
        <button class="reveal-btn" onclick="toggleReveal('r4')">▶ Ver a resolução</button>
        <div class="reveal-content" id="r4">
          <div class="steps">
            <div class="step-item">
              <div class="step-num">1</div>
              <div class="step-content">
                <strong>Fatorar (fator comum)</strong>
                <span>x² + 8x = x · (x + 8)</span>
              </div>
            </div>
            <div class="step-item">
              <div class="step-num">2</div>
              <div class="step-content">
                <strong>Produto zero</strong>
                <span>x · (x + 8) = 0<br>→ x = 0 &nbsp;<strong>ou</strong>&nbsp; x + 8 = 0</span>
              </div>
            </div>
            <div class="step-item">
              <div class="step-num">3</div>
              <div class="step-content">
                <strong>Soluções</strong>
                <span>x = 0 &nbsp;&nbsp;&nbsp; ou &nbsp;&nbsp;&nbsp; x = −8</span>
              </div>
            </div>
          </div>
          <div class="math-block">S = {0 ; −8}</div>
        </div>
      </div>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" onclick="goTo(3)">← Anterior</button>
      <button class="nav-btn next" onclick="goTo(5)">Vamos praticar →</button>
    </div>
  </div>

  <!-- ===== SLIDE 5: Praticando ===== -->
  <div class="slide" id="slide-5">
    <div class="card">
      <div class="card-title"><div class="icon">✏️</div> Hora de praticar!</div>

      <p class="txt">Resolva as equações abaixo usando fatoração. Clique em <strong>"Ver resposta"</strong> quando terminar cada uma.</p>

      <div class="practice-card">
        <h3>Exercício 1 &nbsp;·&nbsp; x² − 25 = 0</h3>
        <p class="txt">Dica: diferença de quadrados! Identifique a = x e b = 5, depois aplique a fórmula e o produto zero.</p>
        <div class="reveal-area">
          <button class="reveal-btn" onclick="toggleReveal('p3')" style="background:var(--green);">✔ Ver resposta</button>
          <div class="reveal-content" id="p3">
            <div class="steps">
              <div class="step-item">
                <div class="step-num">1</div>
                <div class="step-content"><strong>Fatorar (diferença de quadrados)</strong><span>x² − 25 = x² − 5² = (x + 5) · (x − 5)</span></div>
              </div>
              <div class="step-item">
                <div class="step-num">2</div>
                <div class="step-content"><strong>Produto zero</strong><span>x + 5 = 0 → x = −5<br>x − 5 = 0 → x = 5</span></div>
              </div>
              <div class="step-item">
                <div class="step-num">3</div>
                <div class="step-content"><strong>Conferir com x = 5</strong><span>(5)² − 25 = 25 − 25 = 0 ✓</span></div>
              </div>
            </div>
            <div class="math-block">S = {−5 ; 5}</div>
          </div>
        </div>
      </div>

      <div class="practice-card">
        <h3>Exercício 2 &nbsp;·&nbsp; 3x² − 3x − 18 = 0</h3>
        <p class="txt">Dica: divida tudo por 3 primeiro, depois fatore o trinômio com a técnica dos pares. <em>(Este é o estilo da Q5 da AV2!)</em></p>
        <div class="reveal-area">
          <button class="reveal-btn" onclick="toggleReveal('p4')" style="background:var(--accent);">✔ Ver resposta</button>
          <div class="reveal-content" id="p4">
            <div class="steps">
              <div class="step-item">
                <div class="step-num">1</div>
                <div class="step-content"><strong>Dividir tudo por 3</strong><span>3x² − 3x − 18 = 0 &nbsp;÷ 3&nbsp; → x² − x − 6 = 0</span></div>
              </div>
              <div class="step-item">
                <div class="step-num">2</div>
                <div class="step-content"><strong>Encontrar o par: produto = −6 e soma = −1</strong><span>−3 × 2 = −6 ✓ &nbsp;&nbsp; e &nbsp;&nbsp; −3 + 2 = −1 ✓</span></div>
              </div>
              <div class="step-item">
                <div class="step-num">3</div>
                <div class="step-content"><strong>Forma fatorada</strong><span>x² − x − 6 = (x − 3) · (x + 2)</span></div>
              </div>
              <div class="step-item">
                <div class="step-num">4</div>
                <div class="step-content"><strong>Produto zero</strong><span>x − 3 = 0 → x = 3<br>x + 2 = 0 → x = −2</span></div>
              </div>
            </div>
            <div class="math-block">S = {3 ; −2}</div>
            <div class="box box-green">🎯 Esse é exatamente o estilo da Questão 5 da AV2!</div>
          </div>
        </div>
      </div>

      <div class="box box-yellow" style="margin-top:24px;">
        🏆 <strong>Resumo da aula de hoje:</strong><br>
        <span class="chip">Fator comum</span>
        <span class="chip">Diferença de quadrados</span>
        <span class="chip">Fatoração do trinômio</span>
        <span class="chip">Produto zero → raízes</span>
      </div>
    </div>

    <div class="card final-card">
      <div class="big-emoji">🎯</div>
      <h2>Ótimo trabalho!</h2>
      <p>Na próxima aula vamos aprender a <strong>Fórmula de Bhaskara</strong> — para quando a fatoração não é tão direta. Mas tudo que aprendemos hoje é a base!</p>
    </div>

    <div class="nav-btns">
      <button class="nav-btn prev" onclick="goTo(4)">← Anterior</button>
      <button class="nav-btn next" disabled>Fim da aula ✓</button>
    </div>
  </div>

</div><!-- /wrapper -->

<script>
  // FLOATING SYMBOLS
  const symbols = ['x²','ax','+','=','0','b²','√','(x)','−','·','2x','c','Δ','x','{}'];
  const container = document.getElementById('bgSyms');
  for(let i=0;i<28;i++){
    const el = document.createElement('div');
    el.className = 'sym';
    el.textContent = symbols[Math.floor(Math.random()*symbols.length)];
    el.style.left = Math.random()*100 + '%';
    el.style.fontSize = (20 + Math.random()*60) + 'px';
    el.style.animationDuration = (18 + Math.random()*30) + 's';
    el.style.animationDelay = (-Math.random()*40) + 's';
    container.appendChild(el);
  }

  // NAVIGATION
  let current = 0;
  const total = 6;

  function goTo(n){
    document.getElementById('slide-'+current).classList.remove('active');
    const btns = document.querySelectorAll('.step-btn');
    btns[current].classList.remove('active');
    btns[current].classList.add('done');
    current = n;
    document.getElementById('slide-'+current).classList.add('active');
    btns.forEach((b,i)=>{ b.classList.remove('active'); if(i===current) b.classList.add('active'); });
    window.scrollTo({top:0, behavior:'smooth'});
  }

  function toggleReveal(id){
    const el = document.getElementById(id);
    el.classList.toggle('open');
    const btn = el.previousElementSibling;
    btn.textContent = el.classList.contains('open') ? '▼ Fechar' : btn.textContent.replace('▼ Fechar','▶ Ver');
  }
</script>
</body>
</html>
